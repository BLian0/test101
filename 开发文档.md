# Campus Match 开发文档

本文档面向两类读者：

- 人类开发者：需要快速理解项目结构、运行方式和当前业务规则。
- Agent 开发者：需要在尽量少的上下文里接手修改，并避免重复踩坑。

## 1. 项目概览

当前项目按 monorepo 方式放在同一个工作区下，核心包含 3 个子项目：

- `campus-match-frontend`
  - 用户端，基于 `uni-app + Vue 3 + Pinia`
  - 负责注册、资料编辑、问卷、首页推荐、个人页
- `campus-match-backend`
  - 后端，基于 `NestJS`
  - 负责认证、用户资料、问卷结果、匹配推荐、管理端数据接口
- `campus-match-admin`
  - 管理端，基于 `Vue 3 + Vite`
  - 负责用户列表、详情、封禁等后台操作

工作区根目录还有两个辅助启动脚本：

- `start-campus-match.ps1`
- `start-campus-match.bat`

## 2. 当前业务链路

### 用户链路

当前 onboarding 主链路是：

1. 注册
2. 编辑基础资料
3. 填写问卷
4. 进入首页推荐

说明：

- 旧的“偏好页”已经从主流程中移除，当前不再要求用户单独填写偏好。
- 用户完成问卷后直接进入首页，不再跳转到偏好页。
- 资料页只负责基础展示信息，不再重复收集已经由量表接管的数据。

### 资料页职责

当前资料页只保留以下字段：

- 昵称
- 头像
- 年龄
- 自我介绍

以下字段不再由资料页重复录入，因为已经纳入问卷链路：

- 性别
- 性取向
- 月均开支

### 问卷职责

问卷现在不仅负责人格/偏好量表，也负责写回部分 profile 字段。

前置题包括：

- `P01` 性别
- `P02` 性取向
- `P03` 月均开支区间

后续量表题为：

- `Q01` 到 `Q25`

提交问卷后，后端会把 `P01/P02/P03` 解析并写回用户资料模型中的：

- `gender`
- `sexualOrientation`
- `monthlySpending`

这意味着：

- 用户不需要再单独填写偏好页
- 用户不需要在资料页重复填写性别、性取向、开支

## 3. UID 规则

当前用户 UID 已统一为 8 位数字，规则如下：

- 前两位固定为年份前缀：`26`
- 后六位为顺序号，从 `000001` 开始递增

示例：

- `26000001`
- `26000002`

注意：

- 前后端展示时应统一使用 `UID`
- 管理端、个人页等场景中，原先的 `ID` 文案应视为已废弃
- 后端 SQLite / MySQL 两套状态层都已经按该规则生成和修正 UID

## 4. 各子项目职责与关键文件

### 4.1 用户端 `campus-match-frontend`

关键目录：

- `src/pages/onboarding/register`
  - 注册页
- `src/pages/onboarding/basic-profile`
  - 基础资料编辑页
- `src/pages/onboarding/questionnaire`
  - 问卷页
- `src/pages/profile`
  - 我的资料页
- `src/stores/onboarding.ts`
  - onboarding 流转状态
- `src/utils/question-bank.ts`
  - 前端本地题库与问卷工具函数
- `src/services/onboarding.ts`
  - 问卷/资料相关接口定义

当前实现要点：

- 问卷页默认走前端本地题库渲染
- 编辑资料后可返回“我的资料”页，不会强制跳回问卷
- 昵称提示文案是“昵称仅可修改一次”
- 头像通过上传与图片预览处理，不再直接暴露“头像地址”输入框
- 个人页当前展示 `UID / 昵称 / 邮箱`

### 4.2 后端 `campus-match-backend`

关键目录：

- `src/modules/auth`
  - 认证、当前用户状态
- `src/modules/user`
  - 用户资料读写
- `src/modules/questionnaire`
  - 问卷题目、提交、结果聚合
- `src/modules/match`
  - 推荐与匹配
- `src/common/in-memory-app-state.service.ts`
  - 本地/SQLite 状态层
- `src/common/mysql-app-state.service.ts`
  - MySQL 状态层
- `prisma/schema.prisma`
  - 数据结构定义

当前实现要点：

- 资料更新接口允许部分字段更新
- 如果某些 profile 字段未传，后端会保留原值，不会强制清空
- 问卷提交会同步写回 `gender / sexualOrientation / monthlySpending`
- 推荐逻辑已经接入基于资料的硬筛选，不再只依赖旧的弱偏好加权

### 4.3 管理端 `campus-match-admin`

关键文件：

- `src/App.vue`

当前实现要点：

- 用户列表和详情以 `UID` 为主
- 当前主要展示：
  - UID
  - 昵称
  - 邮箱
- 学校字段当前不作为核心展示项
- 用户封禁/解封等操作文案已改为使用 UID

## 5. 问卷题库的当前策略

当前题库采用“前端本地题库 + 后端结果处理”的组合策略。

实际行为如下：

- 前端本地题库定义在 `campus-match-frontend/src/utils/question-bank.ts`
- 后端 `getActiveQuestions()` 当前返回空 `items`
- 前端检测到后端没有可用题目时，会回退到本地题库渲染
- 用户提交后，后端仍会正常计算问卷结果并写回资料字段

这样做的目的：

- 先保证业务链路可测
- 避免后端题目接口与中文题库内容继续互相牵扯，导致编码/补丁冲突

如果后续要进一步收敛架构，建议目标是：

1. 后端输出完整正式题库
2. 前端仅负责渲染
3. 前后端共用统一题目 ID 与枚举定义

## 6. 运行方式

### 6.1 一键启动

在工作区根目录执行：

```powershell
.\start-campus-match.ps1
```

该脚本会尝试：

- 启动 MySQL 容器
- 打开后端开发窗口
- 打开前端开发窗口
- 打开管理端开发窗口

对应访问地址：

- 后端健康检查：`http://localhost:3000/api/v1/health`
- 管理端：`http://localhost:5174`
- 用户前端：查看前端终端输出的实际 H5 地址

前提条件：

- 已安装 Node.js
- 已安装 npm
- 已安装 Docker Desktop，并且 Docker 处于运行状态

### 6.2 分别启动

#### 用户端

```powershell
cd C:\Users\ASUS\Documents\Playground\campus-match-frontend
npm install
npm run dev:h5
```

构建：

```powershell
npm run build
```

#### 后端

```powershell
cd C:\Users\ASUS\Documents\Playground\campus-match-backend
npm install
npm run db:mysql:up
npm run start:dev
```

构建：

```powershell
npm run build
```

测试：

```powershell
npm test -- --runInBand
```

#### 管理端

```powershell
cd C:\Users\ASUS\Documents\Playground\campus-match-admin
npm install
npm run dev
```

构建：

```powershell
npm run build
```

## 7. 当前已验证状态

以下命令已在最近一轮修改后通过：

- `campus-match-frontend`: `npm run build`
- `campus-match-backend`: `npm run build`
- `campus-match-backend`: `npm test -- --runInBand`
- `campus-match-admin`: `npm run build`

说明：

- 当前版本属于“可测试版本”
- 核心主链路已经能跑通，但仍有一些可继续收敛的结构性问题，见下文

## 8. 已知技术债与接手注意事项

### 8.1 题库来源暂时分裂

当前题库内容主要由前端本地文件维护，后端只负责消费答案和产出结果。

影响：

- 修改题库时，优先看 `campus-match-frontend/src/utils/question-bank.ts`
- 若后端未来改为正式下发题库，需要同步清理前端 fallback 逻辑

### 8.2 编码历史问题

部分旧文件存在过中文乱码、历史补丁难以稳定落地的问题。

处理建议：

- 大块中文重写优先直接重写整个文件
- 避免继续在脏编码文件上做零碎 patch
- 对结构性变更，优先用清晰 UTF-8 文件替换旧实现

### 8.3 偏好模块已弱化

产品层面当前已不要求用户额外填写偏好页。

因此后续修改时需要注意：

- 不要把 onboarding 主链路重新接回旧偏好页
- 如果要彻底清理代码，可继续删除前端偏好页面、路由以及后端残留的 `preferencesCompleted` 历史兼容字段

### 8.4 资料与问卷的边界

当前边界已经明确：

- 资料页负责展示型基础信息
- 问卷负责偏向画像/筛选意义的数据

如果未来扩字段，优先先判断它属于哪类：

- 展示信息：放资料页
- 匹配/画像/筛选信息：放问卷

## 9. 给 Agent 的最小接手建议

如果你是新的 agent，不要从零开始扫描整个仓库，优先按这个顺序理解：

1. 先看本文件
2. 看 `campus-match-frontend/src/stores/onboarding.ts`
3. 看 `campus-match-frontend/src/utils/question-bank.ts`
4. 看 `campus-match-frontend/src/pages/onboarding/questionnaire/index.vue`
5. 看 `campus-match-backend/src/modules/questionnaire/questionnaire.service.ts`
6. 看 `campus-match-backend/src/modules/match/match.service.ts`
7. 看 `campus-match-admin/src/App.vue`

接手时的默认假设：

- 主流程是“注册 -> 资料 -> 问卷 -> 首页”
- `UID` 不是数据库自增 ID，而是业务 ID
- 性别 / 性取向 / 开支 现在归问卷，不归资料页
- 不要重新引入旧 demo 题库
- 不要默认恢复旧偏好页

## 10. 建议的后续工作

推荐优先级如下：

1. 把后端题库接口补成完整正式题库，减少前端 fallback
2. 清理前后端对旧偏好模块的历史兼容代码
3. 为问卷结果写回 profile 的链路补更完整的自动化测试
4. 统一前后端的枚举定义，避免 `gender/orientation/spending` 再次漂移
5. 补一份部署文档，明确 dev / test / prod 的环境变量约定

## 11. 提交边界说明

当前工作区 `C:\Users\ASUS\Documents\Playground` 下还有许多与本项目无关的文件和实验目录。

上传到 GitHub 时，建议只纳入以下内容：

- `campus-match-frontend`
- `campus-match-backend`
- `campus-match-admin`
- `start-campus-match.ps1`
- `start-campus-match.bat`
- 本文档 `开发文档.md`

不要把其它无关图片、脚本、临时文件、旧实验目录一并提交。
